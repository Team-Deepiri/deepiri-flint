apiVersion: v1
kind: Service
metadata:
  name: {{ include "bedd.fullname" . }}
spec:
  selector:
    app: bedd
  ports:
    - name: admin
      port: {{ .Values.service.adminPort }}
      targetPort: admin
